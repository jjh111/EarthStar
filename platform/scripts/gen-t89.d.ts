/** Types for the plain-Node generator, so tests can import its parser directly. */
export declare const T89_PARAMS: number;
export declare const T89_BANDS: number;
export declare function parseT89(text: string): number[][];
