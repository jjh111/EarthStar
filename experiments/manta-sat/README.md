# Manta-ray satellite (experiment)

Blender build scripts and scene for a manta-ray LEO satellite concept — an
electromagnetic particle-scoop design. Exploratory; not used by the site or the Viewer.
Run `manta_v2_build.py` inside Blender to regenerate `manta_sat.blend`. Blender's
`.blend1` backups are ignored by git.
